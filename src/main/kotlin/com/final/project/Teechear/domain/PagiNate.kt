package com.final.project.Teechear.domain

data class PagiNate(val currentPage: Int,
                    val lastPage: Int,
                    val pageSize: Int,
                    val perPageSize: Int)
